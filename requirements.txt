# AI Interconnect Latency Benchmark
# Python dependencies

# Core (no external dependencies required for CLI tools)
# All benchmark scripts use only Python standard library

# Optional: Figure Generation
matplotlib>=3.7.0
numpy>=1.24.0

# Optional: Development
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
